/* global define:false */
define(function () {
  'use strict';

  return {
    input: {
      tapMaxDistancePx: 10,
      holdTimeMs: 250,
      swipeMinTimeMs: 50,
      swipeMaxTimeMs: 250,
      swipeDistancePx: 100
    }
  };
});
